package springCourse.core.ioc.testingRunner;

public class TestSpringIoc {
    public static void main(String[] args) {
/*        ClassPathXmlApplicationContext context = new ClassPathXmlApplicationContext(
                "applicationContextIoc.xml");
        //Music music = context.getBean("ClassicalMusicBean", Music.class);
        //MusicPlayer musicPlayer = new MusicPlayer(music);

        ClassicalMusic classicalMusic=context.getBean("ClassicalMusicBean", ClassicalMusic.class);
        ClassicalMusic classicalMusic2=context.getBean("ClassicalMusicBean", ClassicalMusic.class);
        System.out.println("In depends on scope. If it singleton its been the same object->"+(classicalMusic==classicalMusic2));
        System.out.println(classicalMusic.getSong());
        RockMusic rockMusic=context.getBean("RockMusicBean", RockMusic.class);
          MusicPlayer musicPlayer=context.getBean("MusicPlayer", MusicPlayer.class);
        System.out.println(context.getBean("MusicList"));;
        musicPlayer.playMusicList();
        musicPlayer.playMusic();
        System.out.println(musicPlayer.getName());
        System.out.println(musicPlayer.getVolume() );
        context.close();*/
    }
}
